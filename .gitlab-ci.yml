stages:
  - build

build-vsix:
  stage: build
  image: node:20
  script:
    - npm ci
    - npm run compile
    - npm run package
  artifacts:
    paths:
      - "*.vsix"
    expire_in: 90 days
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
